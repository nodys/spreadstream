{
  "name": "spreadstream",
  "version": "0.2.0",
  "description": "Pipe data from and to google spreadsheet",
  "main": "./lib/spreadstream.js",
  "bin": {
    "spreadstream": "./bin/spreadstream.js"
  },
  "scripts": {
    "test": "npm run lint && mocha",
    "test:watch": "mocha -w",
    "coverage": "nyc mocha",
    "coverage:html": "npm run coverage && nyc report --reporter=html",
    "lint": "standard",
    "docs": "cp README.md ./docs/README.md && git commit -m 'doc: Update documentation' ./docs > /dev/null && echo 'doc: Changes commited' || echo 'doc: No change'",
    "gitready": "git diff --exit-code > /dev/null && git diff --cached --exit-code > /dev/null || echo 'Please commit changes first'",
    "release": "npm run docs && npm run gitready && npm test && standard-version",
    "release:major": "npm run release -- --release-as major",
    "release:minor": "npm run release -- --release-as minor",
    "release:patch": "npm run release -- --release-as patch"
  },
  "keywords": [
    "spreadsheet",
    "sheet",
    "google",
    "stream",
    "pipe",
    "json",
    "ndjson",
    "ldjson",
    "csv"
  ],
  "author": "Jean Ponchon <jean.ponchon@novadiscovery.com>",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.5.1",
    "chalk": "^2.3.0",
    "csv-parser": "^1.12.0",
    "csv-write-stream": "^2.0.0",
    "google-auth-library": "^0.11.0",
    "googleapis": "^22.2.0",
    "mississippi": "^1.3.0",
    "ndjson": "^1.5.0",
    "path": "^0.12.7",
    "rc": "^1.2.2",
    "yargs": "^10.0.3"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "docsify-cli": "^4.1.12",
    "mocha": "^4.0.1",
    "nyc": "^11.3.0",
    "standard": "^10.0.3",
    "standard-version": "^4.2.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "standard": {
    "ignore": [
      "/docs/"
    ]
  },
  "bugs": {
    "url": "https://github.com/nodys/spreadstream/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nodys/spreadstream.git"
  }
}
